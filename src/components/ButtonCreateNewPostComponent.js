import styled from "@emotion/styled";
const Button = styled.button`
  background-color: blue;
  color: white;
`;

const ButtonCreateNewPostComponent = () => <Button>Create new post</Button>;

export default ButtonCreateNewPostComponent;
